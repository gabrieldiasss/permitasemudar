import styled from "styled-components";

export const Footer = styled.footer`
    background-color: var(--blue-500);
    text-align: center;

    padding: 1rem 0;

    a {
        color: #000;
        font-size: 2rem;
        font-weight: 600;
    }
`